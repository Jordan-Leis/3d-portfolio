import { describe, it, expect } from 'vitest'

describe('ProjectsPanel content (Phase 4)', () => {
  it('sentinel: describe block is live', () => {
    expect(true).toBe(true)
  })

  it.todo('PROJ-01: renders between 3 and 5 ProjectCard elements')
  it.todo('PROJ-02: each ProjectCard renders title heading (h3)')
  it.todo('PROJ-02: each ProjectCard renders description paragraph')
  it.todo('PROJ-02: each ProjectCard renders at least one tech-stack tag')
  it.todo('PROJ-02: each ProjectCard renders at least one of GitHub link or Demo link with target="_blank" rel="noopener noreferrer"')
})
